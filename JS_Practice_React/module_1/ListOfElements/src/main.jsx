import React from 'react'
import ReactDOM from 'react-dom/client'
import ListOfElements from './ListOfElements.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <ListOfElements />
  </React.StrictMode>,
)
